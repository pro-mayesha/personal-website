# Copy to `.env` in this folder (do not commit real secrets).
# Restart `npm run dev` after changing.

# Optional legacy — not used. Admin ID/password are created at /admin on first visit.
# VITE_ADMIN_PASSWORD=
