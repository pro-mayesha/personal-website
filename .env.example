# Copy to `.env` in this folder (do not commit real secrets).
# Restart `npm run dev` after changing.

# Password for /admin — required to sign in and edit blog posts (stored in browser localStorage).
VITE_ADMIN_PASSWORD=
